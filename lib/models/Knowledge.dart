class Knowledge {
  String name;
  String description;
  int unit;
  int byte;
  String date;
  bool isEnable;
  Knowledge(this.name, this.description, this.unit, this.byte, this.date, this.isEnable);
}