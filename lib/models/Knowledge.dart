class Knowledge {
  String id;
  String name;
  String description;
  int unit;
  int byte;
  String date;
  bool isEnable;
  Knowledge(this.id, this.name, this.description, this.unit, this.byte, this.date, this.isEnable);
}